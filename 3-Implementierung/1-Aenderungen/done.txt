
aus subpackage-Namen wie FacadeModelView den Namensteil entfernt, der dem Namen des VaterPackages entspricht. dem verbliebenen Namensteil ein i vorangestellt.

Beispiel: 
gui.FacadeModelView  -->  gui.imodel

Cache und Backend etwas mehr Struktur gegeben. 

Backend und Cache mit Unterpackages versehen und einige Klassen darin einsortiert, so dass die Module mehr Struktur haben.

getInitBuildingBlock --> getPrototypeBuildingBlock

Timing präzisiert und cache-Klassendiagramm entsprechend leicht angepasst: Attribute der Klassen MRunTiming und MeansForAction. CheckAndNotifyAction benutzt dann jetzt auch MeansForAction. 

